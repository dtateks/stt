# Whiteboard — Windows Runtime Parity Port Plan

> Updated by orchestrator between waves. Executors read, never write directly.
> Contains ONLY actual findings from completed tasks that change downstream work — not planned, not stable shared context.
>
> Management rules:
> - Per-task summary stays concise and downstream-relevant only
> - If whiteboard stops being quickly scannable for downstream executors, archive older wave sections to `whiteboard-archive.md` and add link
> - Orchestrator resolves conflicting discoveries before writing — never leave contradictions for executors
> - **Tie-break for conflicts**: compare Recommendations from each executor's Report. Pick recommendation with stronger Rationale. If equal, prefer the executor whose task is on the critical path. If still equal, pick the more conservative option (less risk to downstream). Document resolution with brief rationale
> - Update Contract Deviations table (cumulative) from executor Contract amendments reports
> - Do NOT restate unchanged `brief.md` guidance or unchanged task-card content
> - If a finding becomes stable cross-cutting truth, promote it into `brief.md`; keep `whiteboard.md` as the delta trail

## Contract Deviations (cumulative — orchestrator maintains)
> Downstream executors Ctrl+F their consumed Contract ID here for instant deviation check.
> Dispatch template tells Wave 2+ executors: use whiteboard deltas as actual state when contract deviations or new downstream constraints conflict with the original plan.
| Contract ID | Planned Signature | Actual Signature | Reason | Affected Consumers |
|-------------|------------------|------------------|--------|--------------------|
| C-01-app-shell-port | `handle_runtime_event(app, event)` (type unspecified) | `handle_runtime_event(app: &AppHandle, event: RunEvent)` | Direct compatibility with `app.run` callback event ownership | 02, 05 |
| C-01-helper-mode-dispatch | `maybe_run_from_args(args: impl IntoIterator<Item = String>) -> Option<i32>` | `maybe_run_from_args<Args>(args: Args) -> Option<i32> where Args: IntoIterator<Item = String>` | Rust stable equivalent for impl-trait ergonomics | 03, 05 |

## After Wave 1

### Task 01: Extract cross-platform shell/runtime contracts
- **Actual outputs**: Created `platform_app_shell.rs`, `platform_runtime_info.rs`, `macos_app_shell.rs`, `windows_app_shell.rs`, `helper_mode.rs`, `runtime_platform_contract.rs`, `platform-runtime-bridge.test.ts`. Modified `lib.rs`, `commands.rs`, `Cargo.toml`, `build.rs`, `default.json`, `bar.json`, `tauri-bridge.js`, `types.ts`. Auto-generated `Cargo.lock`, `gen/schemas/*`, `permissions/autogenerated/get_platform_runtime_info.toml` are expected build side-effects.
- **Contract amendments**: C-01-app-shell-port `handle_runtime_event` concretized to `(app: &AppHandle, event: RunEvent)`. C-01-helper-mode-dispatch uses generic `Args: IntoIterator<Item=String>` instead of `impl Trait`. Both are plan-corrections (type concretization), not behavior changes.
- **New constraints or prerequisites**: None
- **Blockers or failures affecting later work**: None
- **Deviations**: None
- **Discoveries**: (1) Existing `window_shell` and `command_bridge_contract` tests are strict string/allow-list guards — downstream waves adding commands must update allow-lists/capability expectations in lockstep. (2) `platform_app_shell` wraps current runtime without changing command names/payloads — keep Windows behavior changes inside `windows_app_shell.rs` and `platform_runtime_info.rs`.
- **Warnings for later tasks**: Auto-generated artifacts (Cargo.lock, gen/schemas, permissions/autogenerated) are expected build side-effects, not scope violations.
- **Downstream action**: continue

## After Wave 2

### Task 02: Implement Windows shell/HUD/tray runtime
- **Actual outputs**: Modified `src/src/windows_app_shell.rs` (implemented Windows adapter with show/hide/mouse/runtime-event handlers, foreground step, reopen visibility gate). Created `src/tests/windows_shell_runtime.rs` (4 behavioral contract tests using RefCell/closure seams).
- **Contract amendments**: None
- **New constraints or prerequisites**: Tray-assisted recovery requires follow-up runtime composition changes in `lib.rs` (outside Task 02 file ownership).
- **Blockers or failures affecting later work**: None
- **Deviations**: Tray wiring deferred — implemented reopen visibility gate as interim recovery mechanism. Fullscreen HUD behavior is "closest safe Windows behavior" pending VM validation.
- **Discoveries**: (1) Keep Windows `show_bar` on explicit `configure -> position -> show -> front` sequence. (2) Background recovery can use reopen visibility gating even before tray wiring lands.
- **Warnings for later tasks**: VM spot-checks still required for fullscreen overlay, tray recovery UX, and non-focus-stealing HUD behavior under real Windows conditions.
- **Downstream action**: amend — tray-assisted recovery requires plan/runbook update for runtime composition changes

### Task 03: Implement Windows insertion/helper runtime
- **Actual outputs**: Modified `src/src/text_inserter.rs` (Windows cfg-gated routing), `src/src/permissions.rs` (Windows permission backend routing), `src/src/helper_mode.rs` (helper dispatch for `--helper-mode windows-insert`). Created `src/src/windows_inserter.rs` (SendInput/clipboard fallback, elevated helper via PowerShell `Start-Process -Verb RunAs`, hardened temp file transport), `src/src/windows_permissions.rs` (mic, accessibility-equivalent, UIPI checks), `src/tests/windows_text_insertion.rs` (7 behavioral contract tests).
- **Contract amendments**: None
- **New constraints or prerequisites**: Elevated helper path depends on PowerShell `Start-Process -Verb RunAs` availability. Helper IPC uses file-transport flags (`--helper-request-path`, `--helper-response-path`).
- **Blockers or failures affecting later work**: IMP-05 — Windows module inclusion in `text_inserter.rs` lacks cfg-gating, causing dead-code warnings on macOS. Non-blocking, cosmetic.
- **Deviations**: VM/manual elevated-target validation deferred. Windows accessibility-equivalent returns `granted: true` with advisory message (non-blocking) pending VM hardening.
- **Discoveries**: (1) Keep test names prefixed with `windows_text_insertion_` for filter compatibility. (2) Preserve helper text transport over file payloads; never pass dictated text in argv. (3) Treat Windows permission status `microphone: unknown` as advisory, not hard-blocking.
- **Warnings for later tasks**: Windows-specific modules are cfg-gated but `text_inserter.rs` imports them unconditionally (IMP-05). Windows compile/runtime validation still required. Insertion chain reliability is app/context dependent; VM checks mandatory.
- **Downstream action**: continue

### Task 04: Make the UI platform-aware
- **Actual outputs**: Modified `ui/src/main.ts` (runtime info consumption, platform-adaptive permission copy, shortcut labels, background UX, fallback to macOS semantics). Modified `ui/src/startup-permissions.ts` (backend permission detail forwarding). Modified `ui/index.html` (runtime background recovery element). Modified `ui/src/shortcut-display.ts` and `ui/src/shortcut-recorder-logic.ts` (platform-adaptive display). Modified test files to include platform-runtime coverage. Created `ui/src/__tests__/platform-runtime-ui.test.ts`.
- **Contract amendments**: None
- **New constraints or prerequisites**: `main.ts` assumes `getPlatformRuntimeInfo()` may fail and falls back to macOS runtime semantics.
- **Blockers or failures affecting later work**: None
- **Deviations**: None
- **Discoveries**: (1) Drive permission wording from backend `PermissionResult.message` first, then append platform settings guidance. (2) Keep canonical shortcut storage tokens unchanged; only vary display via `shortcutDisplay`. (3) jsdom main-window tests should flush microtasks and macrotasks for async permission/banner updates.
- **Warnings for later tasks**: `renderShortcutRecorderState` has CRITICAL upstream impact; keep its call contract stable. `ui/index.html` now includes `#runtime-background-recovery`; tests parsing production HTML must keep that node intact.
- **Downstream action**: continue

## After Wave 3

### Task 05: Reconcile contracts and verification sweep
- **Actual outputs**: Modified `src/tests/window_shell.rs` (updated shell routing invariant to assert `platform_app_shell` delegation, added `get_platform_runtime_info` capability requirements for both windows). Modified `src/tests/text_insertion_permissions.rs` (added Windows-specific permission code normalization coverage). Modified `src/tests/command_bridge_contract.rs` (added `get_platform_runtime_info` to command contract). Modified `ui/src/__tests__/bridge-contract.test.ts` (added runtime info bridge coverage). Modified `ui/src/__tests__/bar-session-controller.test.ts` (added `getPlatformRuntimeInfo` to bridge mocks).
- **Contract amendments**: None
- **New constraints or prerequisites**: Shared contract tests must now treat `get_platform_runtime_info` as a required command/capability on both `main` and `bar`. Bar-controller bridge mocks must include `getPlatformRuntimeInfo`.
- **Blockers or failures affecting later work**: None
- **Deviations**: Shell routing invariant corrected from direct panel helper calls to `platform_app_shell` delegation (matches actual runtime contract layering).
- **Discoveries**: (1) Validate shell routing through `platform_app_shell`, not legacy direct helper names. (2) Windows insertion assertions should key off stable helper-error prefixes/codes. (3) Keep capability assertions in lockstep with new bridge commands.
- **Warnings for later tasks**: IMP-05 (Windows module cfg-gating) remains in build warnings outside Task 05 scope. `gitnexus_detect_changes` reported critical/truncated workspace-wide diff due to many uncommitted changes — not a reliable scope signal for this task.
- **Downstream action**: amend — IMP-05 remains in implementation/build warnings

## After Wave 4

### Task 06: Sync AGENTS knowledge with final runtime architecture
- **Actual outputs**:
- **Contract amendments**: None
- **New constraints or prerequisites**:
- **Blockers or failures affecting later work**:
- **Deviations**:
- **Discoveries**:
- **Warnings for later tasks**:
- **Downstream action**:
